import type { ComponentStory } from '@storybook/react';
import SideMenu from './SideMenu';

export const Template: ComponentStory<typeof SideMenu> = (args) => <SideMenu {...args} />;

export const args = {
}