import type { ComponentStory } from '@storybook/react';
import Navigation from './Navigation';

export const Template: ComponentStory<typeof Navigation> = (args) => <Navigation {...args} />;

export const args = {
}