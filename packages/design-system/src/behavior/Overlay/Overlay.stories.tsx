import type { ComponentStory } from '@storybook/react';
import Overlay from './Overlay';

export const Template: ComponentStory<typeof Overlay> = (args) => <Overlay {...args} />;

export const args = {
}