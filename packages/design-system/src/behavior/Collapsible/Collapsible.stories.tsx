import type { ComponentStory } from '@storybook/react';
import Collapsible from './Collapsible';

export const Template: ComponentStory<typeof Collapsible> = (args) => <Collapsible {...args} />;

export const args = {
}